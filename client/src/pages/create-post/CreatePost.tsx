import React from 'react';
import { Container } from '@material-ui/core';

export const CreatePost: React.FC = () => {
    return (
        <Container className="heyo" style={{ height: '1000px' }}>
            <code>Hey</code>
        </Container>
    );
};
